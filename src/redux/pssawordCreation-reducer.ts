export type PssawordCreationReducerType = {};

let internalState: PssawordCreationReducerType = {};

const pssawordCreationReducer = (state: PssawordCreationReducerType = internalState, action: any) => {

  switch (action.type) {
    default:
      return state;
  }
}

export default pssawordCreationReducer