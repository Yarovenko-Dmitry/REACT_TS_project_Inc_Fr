export type PasswordRecoveryReducerType = {};

let internalState: PasswordRecoveryReducerType = {};

const passwordRecoveryReducer = (state: PasswordRecoveryReducerType = internalState, action: any) => {

  switch (action.type) {
    default:
      return state;
  }
}

export default passwordRecoveryReducer